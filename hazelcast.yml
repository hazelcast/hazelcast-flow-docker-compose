hazelcast:
  cluster-name: flow
  jet:
    enabled: true
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        member-list:
          - hazelcast-flow-docker-compose-flow-1:5701
          - hazelcast-flow-docker-compose-flow-2:5701
          - hazelcast-flow-docker-compose-flow-3:5701